version: "3.8"

services:
  fern-docs-self-served:
    build:
      context: ../..
      dockerfile: servers/self-served/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
