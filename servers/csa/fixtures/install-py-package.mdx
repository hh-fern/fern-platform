---
title: Install Package
---

This is an example of a code snippet that will only work once a package has been installed.

```bash
pip install -U quote
```

```python
from quote import quote

search = "Jasper Fforde"
result = quote(search, limit=2)
print(result)
```
