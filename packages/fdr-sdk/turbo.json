{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "compile": {
      "outputs": ["dist/**"],
      "dependsOn": [
        "^build",
        "^compile",
        "codegen",
        "@fern-api/snippets#compile"
      ]
    }
  }
}
