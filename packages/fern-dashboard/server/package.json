{
  "name": "server",
  "version": "1.0.0",
  "description": "Fern Dashboard Server with Prisma Database Management",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm compile",
    "codegen": "prisma generate",
    "compile": "pnpm codegen && tsc --build",
    "db:format": "prisma format",
    "db:generate": "prisma generate",
    "db:lint": "prisma lint",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:reset": "prisma migrate reset",
    "db:migrate:status": "prisma migrate status",
    "db:pull": "prisma db pull",
    "db:push": "prisma db push",
    "db:push:test": "./scripts/run-push-test.sh",
    "db:seed:test": "./scripts/run-seed-test.sh",
    "db:setup:test": "bash scripts/setup-test-db.sh",
    "db:studio": "prisma studio",
    "db:test": "./scripts/run-test-db.sh",
    "db:validate": "prisma validate",
    "dev": "pnpm db:generate && ts-node src/index.ts",
    "format": "prisma format",
    "start": "node dist/index.js",
    "test": "./scripts/run-tests.sh"
  },
  "dependencies": {
    "@prisma/client": "5.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.28",
    "dotenv-cli": "^6.0.0",
    "prisma": "5.13.0",
    "ts-node": "^10.9.2",
    "typescript": "5.7.3",
    "vitest": "^1.0.0"
  }
}
